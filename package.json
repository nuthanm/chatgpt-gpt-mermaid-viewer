{
  "name": "mermaid-render",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && npx puppeteer browsers install chrome-headless-shell"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.6.0",
    "body-parser": "^2.2.0",
    "express": "^5.1.0"
  }
}
